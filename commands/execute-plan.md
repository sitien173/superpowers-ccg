---
description: Run one active or named plan phase through Plan → Execute → Review.
argument-hint: [plan directory, slug, or phase number; optional]
---

Invoke `superpowers-ccg:executing-plans`. Require folder layout. Resume from
`.handover.md` and matching OpenMCP jobs before loading guidance for a new phase.
After the final phase, invoke `superpowers-ccg:verifying-before-completion`.

Target: $ARGUMENTS
