---
description: CCG phase orchestration workflow - Claude planner/reviewer/integrator with CP1 canonical routing matrix and tiebreakers
alwaysApply: true
---

## CCG Orchestration Rules

Claude is the **planner, orchestrator, reviewer, and integrator**. It routes execution, reviews phase output, and runs integration checks.

### Checkpoint Protocol

1. **CP0** - Optional selective `docs/wiki/` durable knowledge lookup, then mandatory context-retrieval via `codebase-retrieval` for current local codebase context before CP1 on every task (optionally run `stellaris search_code` in parallel as secondary source for code-specific symbol-level precision); if `codebase-retrieval` fails, output `BLOCKED` and stop (do not use file tools, Grok Search, or executors instead); stellaris failure does NOT trigger `BLOCKED`; Grok Search remains external/current-only after local retrieval succeeds
2. **CP1** - Phase assessment and routing using the routing matrix
3. **CP2** - External execution via Codex/Gemini with final file output
4. **CP3** - Reconciliation when needed (conflicts, gaps, clarifications)
5. **CP4** - Phase review against original request, CP1 success criteria, reviewer checklist, and integration checks

### Routing Matrix

| Task Category | Model | Cross-Validation | Notes / Triggers |
| --- | --- | --- | --- |
| Backend / Logic / API | Codex | No | Default implementation route |
| Tests / CI / Terminal / Infra-DevOps | Codex | No | Terminal-Bench leader |
| Large refactor (>=10 files or >1K LOC) | Codex | No | 7-hr horizon |
| Bug fix / Debugging / Performance | Codex | No | Snappy small + sustained deep |
| Data / ML / Analytics | Codex | No | Logic-heavy |
| UI components / CSS / animation / canvas / SVG | Gemini | No | WebDev Arena leader |
| Multimodal input -> code | Gemini | No | Only multimodal frontier |
| Large-context sweep (>200K tokens) | Gemini | No | 1M ctx, cheapest tier |
| Visual regression / screen automation / OCR | Gemini | No | ScreenSpot-Pro 72.7% |
| Doc / spec extraction from PDFs / diagrams | Gemini | No | Document understanding |
| Security / compliance / legal-sensitive code | Codex | No (mandatory Claude review gate) | Hallucination guardrail |
| Architecture conflict / multi-domain | Cross-Validation (Codex + Gemini) | Yes | Rare arbitration |
| Docs / Comments / Coordination / Simple edits | Claude | No | Per user constraint |
| Orchestration / Review / Integration / Planning | Claude | No | Per user constraint |
| Uncategorized / Ambiguous | Claude | No | Fail-closed; clarify |

### New Routing Axes

1. Context size: >200K tokens routes to Gemini.
2. Multimodal input: screenshots, PDFs, video, and design mocks route to Gemini.
3. Horizon length: >1 hour autonomous chain routes to Codex.

### Tiebreaker Order

1. Hallucination-sensitive -> Codex + mandatory Claude review gate
2. Multimodal input -> Gemini
3. Context >200K -> Gemini
4. UI-dominant -> Gemini
5. Else -> Codex

### External Response Protocol v1.1

All external model calls must:
- Edit files directly via MCP write tools (the on-disk files are the source of truth)
- Respond using ERP v1.1 with `## FILES MODIFIED` listing every changed file, but no duplicated file content
- Never return prototypes or design prose

### Failure Handling

If Codex or Gemini MCP execution fails, output `BLOCKED` immediately and ask the human to retry or explicitly consent to an alternate route. Do not retry, switch executors, spawn subagents/Task/Agent fallback, dispatch another worker, or handle implementation directly without explicit human consent after the block.
