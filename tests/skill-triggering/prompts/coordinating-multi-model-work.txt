I'm working on a change that touches both backend API logic and a React component UI.

Please decide whether you need to invoke Codex and/or Gemini using the codeagent-wrapper tool, then proceed.

Constraints:
- If you decide an external model is required, you MUST invoke it via codeagent-wrapper.
- If the external call is unavailable or times out, you must stop and report BLOCKED.
