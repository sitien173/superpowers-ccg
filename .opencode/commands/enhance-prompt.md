---
description: Enhance a prompt with codebase context and conventions before routing it through the workflow
---

Use the `use_skill` tool to load `superpowers:enhance-prompt`, then follow that skill exactly.

The prompt to enhance is:
$ARGUMENTS
